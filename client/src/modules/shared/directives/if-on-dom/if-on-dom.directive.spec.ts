import { IfOnDomDirective } from './if-on-dom.directive';

describe('IfOnDomDirective', () => {
  it('should create an instance', () => {
    const directive = new IfOnDomDirective();
    expect(directive).toBeTruthy();
  });
});
